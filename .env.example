# Copy this file to .env and fill in your values.
# The application reads these via application.conf substitution (${?VAR_NAME}).

# Server
PORT=8080

# Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Redis
REDIS_URI=redis://localhost:6379
